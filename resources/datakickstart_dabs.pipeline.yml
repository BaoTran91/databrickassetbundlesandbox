# The main pipeline for datakickstart_dabs
resources:
  pipelines:
    datakickstart_dabs_pipeline:
      name: datakickstart_dabs_pipeline_${bundle.target}
      catalog: sandbox
      schema: assetbundle_tutorial_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
        db_env: ${bundle.target}
