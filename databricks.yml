# This is a Databricks asset bundle definition for datakickstart_dabs.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: datakickstart_dabs
  uuid: d4e8e3e6-0184-42ab-bc83-ed616daa1022

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-11d28957-3f38.cloud.databricks.com
  staging:
    mode: production
    workspace:
      host: https://dbc-11d28957-3f38.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/bqt1991@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/bqt1991@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: bqt1991@gmail.com
        level: CAN_MANAGE
  prod:
    mode: production
    workspace:
      host: https://dbc-11d28957-3f38.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/bqt1991@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/bqt1991@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: bqt1991@gmail.com
        level: CAN_MANAGE
